package com.sist.manager;
/*
 *   1. 클래스는 한 개의 모든 기능
 *      - 관련된 클래스 => 폴더에 모음: 패키지
 *        ==> 찾기 쉽게 만들어야 함
 *   2. 패키지에 있는 클래스를 가지고 올때 
 *      같은 패키지인 경우 그냥 사용
 *      다른 패키지면 import 사용
 *      
 *   자바의 코딩 순서
 *   package : 한 번만 사용
 *   |
 *   import : 여러 개 사용 가능 => 모든 클래스 불러오기: .*
 *   |                      => 클래스 하나 불러오기: .클래스명
 *   |
 *   public class ClassName
 *   {
 *     변수 설정  ==> 순서 상관 없음
 *     생성자 설정
 *     메소드 설정
 *     main(){}
 *   }
 */
public class SawonManager {

}
